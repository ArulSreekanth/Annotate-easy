# Copy this file to .env (which is gitignored) and fill in real values.
# .env is loaded automatically on startup and is never committed.

# Required. Generate one with: openssl rand -base64 24
ANNOTATE_PASSWORD=

# Optional overrides -- uncomment to change from the defaults in app.py
# ANNOTATE_DEVICE=auto
# ANNOTATE_MODEL_TYPE=vit_h
# ANNOTATE_CHECKPOINT=../checkpoints/sam_vit_h_4b8939.pth
# ANNOTATE_SESSION_TTL=1800
# ANNOTATE_BASE_MODEL=yolov8n.pt
# ANNOTATE_BASE_MODEL_CONF=0.25
# ANNOTATE_BASE_MODEL_MAX_DETECTIONS=50
