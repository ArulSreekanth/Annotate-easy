fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
python-dotenv==1.2.3
pydantic==2.11.7
numpy==2.2.6
opencv-python-headless==4.11.0.86
pillow==11.0.0
torch==2.7.1
torchvision==0.22.1
segment-anything==1.0
ultralytics==8.4.131
# ultralytics depends on opencv-python (GUI build), which reinstalls over
# opencv-python-headless above and pulls in libGL etc. we don't need on a
# server. After `pip install -r requirements.txt`, run:
#   pip uninstall -y opencv-python && pip install --force-reinstall --no-deps opencv-python-headless==4.11.0.86
